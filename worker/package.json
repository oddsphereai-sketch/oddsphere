{
  "name": "oddsphere-stream-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Persistent SharpAPI WebSocket market-intelligence worker (host-portable: Render now, Fly later).",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "tsx": "^4.22.3",
    "typescript": "^5"
  }
}
