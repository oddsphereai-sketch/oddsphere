# Phase 4 — NHL Automation + Tracking Audit

**Date:** 2026-06-10
**Auditor:** Phase 4 of the site-wide reliability audit
**Scope:** NHL across automation, market/line ingestion, public/sharp/movement, prediction generation, DTO/card truthfulness, final-score ingest, grading/tracking, calibration, and per-component verdict.
**Method:** Read-only. Every claim cites `file:line` or DB query result.

---

## Summary verdict

NHL is **PARTIAL — Stanley Cup only — TRACKING WORKED for the one verified game, but cannot be called TRUSTED at scale.**

What works:
- Cron is wired (`/api/cron/nhl-daily-refresh` at 13:45 UTC daily) and gated by `NHL_CRON_ENABLED`.
- Slate seeding from NHL.com `/v1/schedule/{date}` via `lib/services/nhl/seedNhlGamesService.ts`.
- Line ingest for ML + Total + Spread (puck-line) into `lines` with per-sportsbook DELETE scope.
- A v0 rule-seeded model (`lib/automodel/nhlAutoModelV0.ts`) producing ML / Total / puck-line.
- Score ingest via `lib/services/nhl/nhlScoreIngestService.ts` with ET-timezone handling.
- Grading via `lib/services/nhl/gradeNhlPredictions.ts` — verified end-to-end for the one game in the system: **CAR ML = WIN, OVER 5.5 = WIN, final 5-3.**

What does not work or is missing:
- **NHL sharp_signals: 0 rows** for the one NHL game in the system. The writer is wired sport-agnostically in `automationOrchestrator.ts:941-951`, but no signal data actually landed for `game.id=15204`. **Could be a vendor coverage gap (SharpAPI NHL Finals splits sparse), a provider-client bug, or an orchestrator-step skip.** Phase-6 must investigate.
- **NHL locked snapshot is THIN.** 7 top-level keys vs MLB's 47. No `predicted_home_score` / `predicted_away_score` / `predicted_total`, no `signal_rows_at_lock`, no `lines_at_lock`, no `data_integrity`, no `framework_grades_at_lock`, no `public_splits`, no `line_movement`. NHL uses its own `market_at_lock.lines_snapshot` shape — incompatible with MLB.
- **NHL puck-line is generated AND displayed but NOT internally captured at lock AND NOT labeled as context-only.** Per `lib/services/nhl/buildNhlPredictionRecords.ts:7-11`. Per the public-tracking-vs-internal-audit rule (see §A.4 below), the correct framing is **not** that puck-line "must be added to prediction_records" — that would auto-include it in public tracking, which we have not historically tracked. The correct framing is: **if displayed, puck-line must be (a) captured in an internal lock-time snapshot for auditability AND (b) UI-labeled as context-only / not part of official tracking AND visually distinct from ML/Total.** NBA spread was handled via Option-B (hide entirely via `marketKeysFor("nba")` in `cba9ea5`); NHL puck-line currently does neither.
- **NHL goalie selection is heuristic.** `snapshot_json.goalie_assumption.source = "default_most_playoff_gp"` — picks the goalie with the most playoff GP, NOT confirmed starters. Reader-facing copy that implies "our model considered the starting goalie" is technically misleading.
- **NHL DTO has hardcoded honesty gaps:** `sharpSignals: []` (always empty), `sharpRead: "wait_no_edge_clean"` (always this key), `result.markets.pickResult: null` (always null even on graded games), `result.markets.gradeUnits: null` (same).
- **NHL auditor coverage: NONE.** `scripts/operator/audit-daily-edge-integrity.ts:32-38` is hardcoded MLB-only. Cross-sport auditor (task #453) is the prerequisite for trusted NHL.
- **NHL calibration: NOT VALIDATED.** Sample size = 2 graded predictions = 1 game. Statistically meaningless.

---

## A. Scope clarity

### A.1 Is NHL a full-season model or Stanley Cup only?

**Stanley Cup only operationally; season-aware in code.**

- `lib/services/nhl/seedNhlGamesService.ts:111-113` — maps NHL.com `game_type` 1/2/3 to `season_type` preseason/regular/postseason. No `eq("season_type", "postseason")` filter on the seed call.
- `lib/automodel/nhlAutoModelV0.ts:11-24` — explicitly states: *"Stanley Cup playoffs = small sample, high variance → confidence capped at 58% probability for v0."*
- Operationally scoped via `NHL_CRON_ENABLED` env gate (`/api/cron/nhl-daily-refresh/route.ts:44-51`).
- DB reality (live probe 2026-06-10): **`games` table has 1 NHL row** (game.id=15204, CAR@VGK 2026-06-09).

### A.2 Markets supported

| Market | Generated by model? | Persisted to prediction_records? | Tracked / graded? |
|--------|---------------------|----------------------------------|-------------------|
| moneyline | YES | YES | YES (verified) |
| total | YES | YES | YES (verified) |
| puck_line (spread) | YES | **NO** | **NO** |

- `lib/services/nhl/buildNhlPredictionRecords.ts:7-11` (comment): *"Markets: moneyline and total ONLY (per v0 spec; puck-line deferred until calibration justifies it)."*
- Live DB confirmation: NHL `prediction_records` markets = `{"moneyline":1,"total":1}`. Puck-line is absent.

### A.3 Pick style

Label-style picks identical to MLB convention:

- ML: `"CAR ML"` (`pr.id=946`, side=`"away"`, odds=155 → home=3 away=5 → CAR=away won).
- Total: `"OVER 5.5"` (`pr.id=947`, side=`"over"`, odds=-125 → total 8 > 5.5 → won).
- Puck-line (generated, displayed, not officially-tracked): `"VGK +1.5"` (from `model_output.puck_line.pick`).

### A.4 Public-tracking-vs-internal-audit rule (applies to puck-line + any future context market)

Daniel codified this rule 2026-06-10: "tracked" has two meanings that must not be conflated.

| Concept | Scope | Today's markets |
|---------|-------|-----------------|
| **Public tracking** (member-facing accuracy section) | INTENTIONAL — only markets we have historically + deliberately launched | MLB ML+Total+FI, NBA ML+Total, NHL ML+Total |
| **Internal audit / lock provenance** | UNIVERSAL — every displayed output must be captured at lock | All of the above + every context-only market shown |

**Durable platform rule:** Every displayed model output must be internally auditable. Only intentionally launched markets should be included in public tracking.

**Implication for this audit:** earlier phases (and earlier framing of §F.3.d below) said "puck-line must be persisted as a prediction_record OR hidden." That framing conflates the two concepts because `prediction_records` is the substrate for the public tracking section — adding a row there implicitly extends public tracking. The corrected framing has three valid options:

- **Option A (recommended for puck-line going forward):** add an internal `displayed_market_snapshot` (or `snapshot_json.displayed_context_markets.puck_line` subfield) capturing pick/line/edge/projection/grade/source at lock time — this is auditable but does NOT join into public tracking — AND label the UI chip clearly as "Model context · Not part of official tracking", visually distinct from ML/Total.
- **Option B (current state for NBA spread, via `cba9ea5`):** hide entirely from `marketKeysFor(sport)`. No display means no audit needed and no public-tracking inclusion. Conservative but loses the analytical value to readers.
- **Option C (separate product decision):** officially launch puck-line as a publicly tracked market, with sample-size baseline + calibration disclosure. This is a deliberate launch, not an engineering default.

Today NHL puck-line is doing **none of the three.** It is displayed without internal capture and without UI labeling. That is the actual problem — not "needs to be in prediction_records."

---

## B. Schedule / seeding automation

### B.1 NHL daily refresh route

`app/api/cron/nhl-daily-refresh/route.ts` — exists and is fully implemented. Env gate at lines 44/51: returns `{disabled: true, records_updated: 0}` if `process.env.NHL_CRON_ENABLED !== "true"`.

### B.2 Vercel cron entry

`vercel.json` line 93-94 (effective):
```
45 13 * * *  /api/cron/nhl-daily-refresh
```

13:45 UTC = 9:45 AM ET — runs once per day. This is 1 invocation/day vs MLB's 16 slate-cycle invocations/day and NBA's 1.

### B.3 Sport-agnostic crons that ALSO cover NHL

- `0 * * * *  /api/cron/tracking-refresh` (hourly) — sport-agnostic; calls `trackingRefreshService.ts` which dispatches to NHL grading + score ingest (`trackingRefreshService.ts:46`).
- `*/15 * * * *  /api/cron/pregame-sweep` (every 15 min) — sport-agnostic; handles lock contract for all sports.

### B.4 Slate seeding

- Provider client: `lib/providers/nhl/_nhlApiClient.ts` — `getSchedule(date)` calls NHL.com `/v1/schedule/{date}`.
- Service: `lib/services/nhl/seedNhlGamesService.ts` — upserts `teams` and `games` (sport='nhl').
- ET timezone: slate_date stored as ET date; this is the same convention as MLB (handles late tips correctly).

### B.5 Operator scripts (manual fallback)

`scripts/operator/nhl/`:
- `seed-nhl-games.ts`
- `refresh-nhl-lines.ts`
- `refresh-nhl-team-stats.ts`
- `refresh-nhl-goalie-stats.ts`
- `run-nhl-prediction.ts`
- `grade-nhl-games.ts`
- `write-nhl-prediction-records.ts`

These are operator-fallback only; normal Stanley Cup operation runs entirely from the cron path.

---

## C. Market / line ingestion

### C.1 ML, Total, Spread all ingested into `lines`

`lib/services/nhl/refreshNhlLinesService.ts:17-18` (comment): *"Markets: moneyline + total_goals + puck_line (the three game-line markets confirmed in the probe)."*

Market normalization (lines 111-119):
```ts
if (s === "moneyline" || s === "ml" || s === "h2h") return "moneyline";
if (s === "puck_line" || s === "puckline" || s === "spread" || ...) return "spread";
if (s === "total_goals" || s === "totalgoals" || s === "total" || ...) return "total";
```

### C.2 Live evidence

For game.id=15204 (post-game probe, 2026-06-10): **0 rows in `lines` table.** This is because the NHL refresh has not re-run after the game (no need — game is over). The lock-time substrate is preserved in `prediction_records.snapshot_json.market_at_lock.lines_snapshot` (sample shows 8 ML lines from `fliff`, `betrivers`, `ballybet` etc).

This is acceptable: the live `lines` table is for pre-game/in-game decision-making; post-game state lives in the snapshot.

### C.3 line_history scope for NHL

`lib/services/nhl/refreshNhlLinesService.ts:337-358` — appends to `line_history` for every parsed payload (per market, per side, per book). Same shape as MLB.

### C.4 Per-sportsbook DELETE scope

Inherited from the Lines-thinning fix (tasks #429-#432). A refresh with fewer books does not clobber existing books.

### C.5 What is and isn't ingested

| Source | Endpoint | What flows |
|--------|----------|-----------|
| SharpAPI | `/api/v1/odds?league=nhl` | ML + Total + Spread odds per book |
| SharpAPI | `/api/v1/splits?league=nhl` | Public splits per market |
| SharpAPI | `/api/v1/opportunities/ev?league=nhl` | EV proxy (replaces `/signals/sharp-action` which returns 404 for NHL per `_sharpApiNhlClient.ts:1-23`) |
| MoneyPuck | (custom) | Team stats + goalie stats |
| NHL.com | `/v1/schedule`, `/v1/score` | Schedule + scores |

---

## D. Public / sharp / market movement

### D.1 Pipeline IS wired sport-agnostically

`lib/services/automationOrchestrator.ts:941-951` — Step S8 calls `linesService.refreshSharpSignals(opts.sport, opts.date, {...})`. Sport-agnostic, so `sport="nhl"` flows through.

### D.2 BUT — 0 rows of sharp_signals for the one NHL game

Live probe of `sharp_signals` for `game.id=15204`: **0 rows.** Despite the wiring.

Possible causes (Phase-6 must investigate):
1. **Vendor coverage gap.** SharpAPI may not have `/splits` data for the specific NHL Finals matchup. This is the same Stanley Cup variant of the [[project-sharpapi-nba-coverage-gap]] finding for NBA Finals.
2. **Pipeline skip.** The `/api/cron/nhl-daily-refresh` route may not invoke `refreshSharpSignals` for NHL even though the orchestrator path supports it.
3. **Provider client bug.** `lib/providers/nhl/_sharpApiNhlClient.ts` may have a query-shape mismatch for NHL splits.

### D.3 Movement ingestion

Same as MLB and NBA: NHL has the infrastructure (`line_history` + `sharp_signals`) but does NOT compute open→current delta as a model input. The NHL model (`nhlAutoModelV0.ts`) consumes only current-state features.

### D.4 Effect on picks / confidence / grade

With 0 sharp_signals rows for the NHL game:
- Pick: deterministic from model (unaffected — model doesn't consume sharp signals).
- Confidence: capped at 58% (v0 explicit cap).
- play_grade: `pr.id=946` has `play_grade=model_only` — consistent with "no sharp signals available."
- Sharp/public chips in UI: NONE shown (correctly, since no data).

This is **internally consistent** — but only because sharp_signals is empty. If signal data were available but not consumed, that would be an honesty issue. Today the issue is the absence of data.

---

## E. Prediction generation / records

### E.1 Model

`lib/automodel/nhlAutoModelV0.ts` — pure rule-seeded function.

**Layered scoring** (visible in `pr.id=946.snapshot_json.model_output.layers`):
- `home_ice_goals`: +0.15
- `special_teams_goals`: −0.15
- `team_strength_goals`: −0.40
- `rest_advantage_goals`: 0
- `series_context_goals`: 0
- `goalie_advantage_goals`: −0.108
- Derived: `expected_goal_diff`, `expected_total_goals`

**Output structure:**
- `moneyline: {pick, notes, verdict, confidence, probability, model_market_gap_pct}`
- `total: {pick, notes, verdict, confidence, probability, model_market_gap_pct}`
- `puck_line: {pick, notes, verdict, confidence, probability, model_market_gap_pct}`

### E.2 Live record state

**2 NHL prediction_records total** (probed 2026-06-10):

| pr.id | game_id | slate | market | pick | side | conf | grade | line | odds | locked |
|-------|---------|-------|--------|------|------|------|-------|------|------|--------|
| 946 | 15204 | 2026-06-09 | moneyline | CAR ML | away | 0.58 | model_only | null | 155 | YES |
| 947 | 15204 | 2026-06-09 | total | OVER 5.5 | over | 0.52 | market_watch | 5.5 | -125 | YES |

Model version: `nhl_v0_2026_finals`.

### E.3 Pre-lock refresh behavior

Searched `automationOrchestrator.ts` and `trackingRefreshService.ts`. NHL is not given a dedicated pre-lock refresh path; it inherits the sport-agnostic hourly tracking-refresh + 15-minute pregame-sweep. **Adequate** at current scale (1 game/day during Stanley Cup), but it does not give NHL the same refresh density as MLB.

### E.4 Snapshot completeness — THIN vs MLB

`pr.id=946.snapshot_json` has **7 top-level keys**:

| Key | Present? | Value sample |
|-----|----------|--------------|
| `model_output` | YES | full v0 ML+Total+PuckLine result |
| `feature_inputs` | YES | 4 sub-keys: away, home, market, series |
| `market_at_lock` | YES | total_line=5.5, ml_book_count=3, lines_snapshot[8] |
| `goalie_assumption` | YES | `{away:{source:"default_most_playoff_gp", player_name:"Frederik Andersen"}, home:{..., "Carter Hart"}}` |
| `locked_at_iso` | YES | `2026-06-09T23:01:40.979Z` |
| `lock_source` | YES | `"locked"` |
| `model_version` | YES | `"nhl_v0_2026_finals"` |

Compare to MLB's 47 keys (Phase 3 §A.2). **NHL is missing:**

| Field (per MLB benchmark) | NHL has? | Note |
|--------------------------|----------|------|
| `predicted_home_score` / `predicted_away_score` | **NO** | Model outputs `expected_goal_diff` only, not per-team scores |
| `predicted_total` | INDIRECT | `model_output.total.probability` exists; raw `expected_total_goals` is in `model_output.layers` but no top-level `predicted_total` |
| `signal_rows_at_lock` | **NO** | NHL uses `market_at_lock.lines_snapshot` instead — different shape |
| `lines_at_lock` | INDIRECT | `market_at_lock.lines_snapshot` is the equivalent — non-standard shape |
| `data_integrity` | **NO** | No 13-key integrity block |
| `framework_grades_at_lock` | **NO** | Different grade model |
| `public_splits` | **NO** | No splits substrate persisted |
| `line_movement` | **NO** | No movement block |
| `model_integrity_notes` | **NO** | No notes array |
| `splits_state` | **NO** | No categorical |
| `current_price` | **NO** | NBA-style honesty field; absent |

### E.5 Goalie assumption is heuristic

`snapshot_json.goalie_assumption.source = "default_most_playoff_gp"` — picks the goalie with the most playoff games played, NOT a confirmed starter.

For the verified game (CAR@VGK 2026-06-09), the model assumed Carter Hart starts at home and Frederik Andersen starts away. The actual starting goalies must be cross-referenced with NHL.com to verify whether this assumption matched reality. **For Phase-6:** NHL needs a real confirmed-goalie ingest (analogous to MLB's confirmed-lineup ingest).

---

## F. DTO / card truthfulness

### F.1 NHL DTO branch

`app/api/lab/daily-edge/route.ts:2698-2719` — `if (sport === "nhl")` branch calls `buildNhlDailyEdgeAdapted()`.

### F.2 Fields shown and their sources

| Field | Source | Truthful? |
|-------|--------|-----------|
| `pick` (e.g., "CAR ML") | `nhlAutoModelV0.{ml,total,puck_line}.pick` | YES |
| `confidence` | `nhlAutoModelV0.{...}.confidence` (0.0–1.0, scaled to 0–100 for UI) | YES |
| `play_grade` / verdict pill | `nhlAutoModelV0.moneyline.verdict` → mapped to 4-cat `{best_angle, lean, watchlist, caution, no_play}` | YES |
| `projected_score` (away/home goals) | Computed: `baseTotal = expected_total_goals/2 ± abs(goal_diff)/2` | YES (formula deterministic; `adaptNhlToDailyEdgeResponse.ts:430-434`) |
| `projected_total` | `expected_total_goals` direct | YES |
| `market_line` (ML odds, Total goals, Puck-line) | `lines` best-price + median-boost filter (3+ candidate books) | YES with caveat (boost filter doesn't activate on 1-2 book markets) |
| `odds_american` | `lines` highest American odds for picked side | YES |
| `sharp/public split chips` | SharpAPI `/splits` payload | YES with honesty guard: returns `[]` if all 4 split values are null |
| `line_movement` (open→current) | `line_history` first-observed price + `lines` current price | YES — calculated from real history |
| `rationale / breakdown` | Hard-coded template filled with model outputs | YES (fact-only narrative) |
| `marketSource` (sportsbook) | `lines.sportsbook` | YES |
| `marketDataQuality` (`"single_book"` / `"unavailable"`) | Derived from book count | YES |
| `sharpSignals` array | **Hardcoded empty `[]`** | **GAP — see F.3** |
| `result.markets.{pickResult, gradeUnits}` | **Hardcoded `null`** | **GAP — see F.3** |
| `breakdown.sharpRead.key` | **Hardcoded `"wait_no_edge_clean"`** | **GAP — see F.3** |

### F.3 Honesty gaps in the NHL DTO

#### Gap F.3.a — `sharpSignals: []` always

`adaptNhlToDailyEdgeResponse.ts:512` — hardcoded empty array. The UI will never render sharp-signal badges for NHL games even if `sharp_signals` rows exist.

**Impact:** today this is consistent with reality (0 sharp_signals rows for the one NHL game). But if the SharpAPI coverage gap closes and rows start landing, the UI will silently ignore them.

#### Gap F.3.b — `result.markets.{pickResult, gradeUnits}: null` always

`adaptNhlToDailyEdgeResponse.ts:522-526` — even for the graded CAR@VGK game (both markets won), the DTO returns `pickResult: null, gradeUnits: null`. **Users can see final score but cannot see whether their pick won or how many units.**

This is the most user-visible NHL honesty gap. Both predictions WON; the DTO does not say so.

#### Gap F.3.c — `breakdown.sharpRead.key: "wait_no_edge_clean"` always

`adaptNhlToDailyEdgeResponse.ts:531-533` — hardcoded. Narrative defaults to "Market and model snapshot reviewed; no decisive sharp signal" regardless of whether sharp data is absent or present (currently absent). **Today this matches reality but cannot adapt when data arrives.**

#### Gap F.3.d — Puck-line displayed in `first_inning` slot without internal capture or context-only labeling

`marketKeysFor("nhl")` returns `["moneyline", "total", "first_inning"]`. The `first_inning` slot is repurposed for puck-line display. The puck-line pick/line/grade are shown in the reader card with the same visual styling as ML/Total.

`lib/services/nhl/buildNhlPredictionRecords.ts:7-11` confirms puck-line is NOT written to `prediction_records`. **Per the public-tracking-vs-internal-audit rule (§A.4), this is correct for public tracking** — we have not historically tracked puck-line and adding rows would pollute the tracking history. **What is missing** is the internal capture (Option A in §A.4): a `displayed_market_snapshot` or `snapshot_json.displayed_context_markets.puck_line` substrate so the auditor can verify what was displayed.

**Impact:** users see a puck-line recommendation in the reader card with the same visual treatment as ML/Total. They cannot tell from the UI that puck-line is **not officially tracked** — they reasonably assume the same audit/grade/accuracy trail applies. The auditor cannot verify what puck-line was shown at lock because nothing is captured. Three valid paths forward (Option A / B / C in §A.4); doing nothing is not one of them.

#### Gap F.3.e — Goalie assumption not surfaced

The reader card does not show "Assumed starting goalies: Andersen (CAR), Hart (VGK) — heuristic, not confirmed." Users who care about starting goalies (and many NHL bettors do) have no way to know the model used a heuristic.

---

## G. Final score / status ingest

### G.1 Service

`lib/services/nhl/nhlScoreIngestService.ts` — reads from NHL.com `/v1/score/{date}`, matches to games, writes final scores + status.

### G.2 ET timezone handling

Lines 79-89 (comment): *"NHL's /v1/score/{date} endpoint indexes by ET (local) game date, NOT UTC. Late-evening tips file under the ET date even when their game_date is UTC-midnight-next-day."*

Fetches both `slate_date` (ET) and `game_date.slice(0,10)` (UTC) to cover both indexing conventions. **Correct.**

### G.3 Status mapping

Lines 117-118, 124 — recognizes `"FINAL"` and `"OFF"` as final states. Live evidence: game.id=15204 status=`"OFF"` (status was correctly written; final scores populated home=3 away=5).

### G.4 Retry / catch-up

Idempotent by design (re-running on a completed game is a no-op). `/api/cron/tracking-refresh` runs hourly, catching any missed ingests.

### G.5 Vercel cron

No separate NHL score-ingest cron. Score ingest runs inside hourly `/api/cron/tracking-refresh`:
- `trackingRefreshService.ts:46` — when `sport="nhl"`, calls `ingestNhlFinalScores()` per date.

This is **adequate** at current scale (1 game/day) but Phase-6 should evaluate whether it scales to multi-game NHL slates.

---

## H. Grading / tracking

### H.1 Grader

`lib/services/nhl/gradeNhlPredictions.ts` — dedicated NHL grader. Handles label-style picks.

### H.2 The CAR@VGK verification

Live DB (probed 2026-06-10):
- game.id=15204, slate_date=2026-06-09, status=OFF, scores home=3 away=5 (CAR=away)
- pr.id=946: market=moneyline, pick="CAR ML", side="away" → result=**WIN** ✓
- pr.id=947: market=total, pick="OVER 5.5", side="over", line=5.5 → total=8 → result=**WIN** ✓
- Both `prediction_grades.win=true`, `loss=false`, `result="win"`

**Tracking works end-to-end for this game.** This closes pending task #450 ("NHL: diagnose game 15204 — was it really scheduled, what was the actual result?") — answer: scheduled, played, CAR won 5-3, both picks WON, both graded correctly.

### H.3 Tracking tie-back

`gradeNhlPredictions.ts:106-114` — joins `prediction_records` (sport='nhl') to `prediction_grades` on `prediction_record_id`. Same join pattern as MLB.

### H.4 Pending-grade UPDATE behavior

Not directly tested in this audit. The Phase 1 cross-sport critical-path audit (`docs/audit/01-active-sports-critical-path.md`) flagged "NHL pending-grade UPDATE" as a known follow-up. Today's NHL sample (n=2) has both rows resolved, so no current pending state to verify.

**Phase-6 must:** add a regression test that simulates a pending grade row + score ingest + grade UPDATE for NHL specifically.

---

## I. Calibration

### I.1 Sample

Total NHL prediction_records: **2**
Graded: **2** (both WIN)
- ML: 1W / 0L (n=1) → 100% accuracy
- Total: 1W / 0L (n=1) → 100% accuracy
- Puck-line: 0 records (not persisted)

### I.2 Verdict

**NOT VALIDATED.** Sample size = 1 game. Statistically meaningless. Two wins prove nothing about the v0 model's accuracy.

A serious NHL calibration audit requires n ≥ 30 per market. At current Stanley Cup-only pace (1 game/day during the Finals), we will not reach that threshold in this regular season.

**Phase-6 options:**
1. Wait for next Stanley Cup Finals → likely 3-7 games each year; multi-year accumulation.
2. Expand scope to full NHL playoffs (5-15 games/day during first rounds, ~80 total per playoffs).
3. Expand scope to regular season → adequate sample, but model is currently calibrated for "Stanley Cup playoffs = small sample, high variance" (per `nhlAutoModelV0.ts:11-24`) — needs re-validation for regular season.

### I.3 No `calibration_buckets` rows for NHL

Phase 3 §K.6 showed `calibration_buckets` has MLB-only rows. NHL has zero entries. The calibration UI does not surface NHL accuracy.

---

## J. Per-component NHL verdict

| Component | Verdict | Evidence |
|-----------|---------|----------|
| **NHL ML pick generation** | WORKS (n=1, NOT VALIDATED) | `nhlAutoModelV0.ts` produces deterministic ML picks; verified WIN on CAR@VGK 2026-06-09 |
| **NHL Total pick generation** | WORKS (n=1, NOT VALIDATED) | Same as above; OVER 5.5 verified WIN |
| **NHL Puck-line / Spread** | DISPLAYED WITHOUT INTERNAL CAPTURE OR CONTEXT-ONLY LABELING | Generated by model and displayed in `first_inning` slot with same visual treatment as ML/Total, but NOT in any internal lock-time snapshot and NOT UI-labeled as not-officially-tracked. Per §A.4 rule, this requires Option A (internal capture + UI label), Option B (hide), or Option C (intentional public launch). NOT adding to `prediction_records` directly — that pollutes public tracking. See §F.3.d |
| **NHL predicted score** | NOT IN SNAPSHOT | No `predicted_home_score`/`away_score`/`predicted_total` at top-level. Model has `expected_goal_diff` + `expected_total_goals` but not split into per-team scores. UI computes projected per-team scores from these in the adapter (`adaptNhlToDailyEdgeResponse.ts:430-434`) but the result is not persisted |
| **NHL market/sharp signal usage** | GAP — 0 rows for the one NHL game | `refreshSharpSignals` wired sport-agnostically (`automationOrchestrator.ts:941-951`); 0 actual rows for game.id=15204. Phase-6 must investigate vendor coverage vs pipeline bug |
| **NHL tracking/grading** | WORKS (verified) | CAR ML WIN + OVER 5.5 WIN both correctly graded. Label-style picks parse correctly (`gradeNhlPredictions.ts`) |
| **NHL automation/cron** | WIRED | `/api/cron/nhl-daily-refresh` at 13:45 UTC + `/api/cron/tracking-refresh` hourly + `/api/cron/pregame-sweep` every 15 min. NHL_CRON_ENABLED gate |
| **NHL auditor coverage** | NONE | `scripts/operator/audit-daily-edge-integrity.ts:32-38` is MLB-only. NHL has zero auditor checks. Cross-sport auditor v2 is task #453 |
| **NHL calibration** | NOT VALIDATED | n=1 game. Statistically meaningless |
| **NHL DTO honesty** | PARTIAL — 3 hardcoded gaps | `sharpSignals: []` always, `result.markets.pickResult: null` always (even on graded games), `sharpRead: "wait_no_edge_clean"` always (see F.3) |
| **NHL goalie assumption** | HEURISTIC | `default_most_playoff_gp` instead of confirmed starters. Not surfaced in UI. Phase-6 needs confirmed-goalie ingest |
| **NHL snapshot completeness** | THIN (7/47 vs MLB) | Missing data_integrity, signal_rows_at_lock, lines_at_lock, framework_grades_at_lock, public_splits, line_movement, predicted_*_score, model_integrity_notes |

---

## K. Important non-claims (NHL)

In the spirit of Phase 3 §M, the following items are **explicitly not yet true** of NHL:

1. **CLV is NOT tracked for NHL.** Same as MLB — no closing-line snapshot job, no CLV computation, no storage, no display.
2. **Market movement (open→current) is NOT a model input for NHL.** Same as MLB and NBA.
3. **Sharp/public signals are NOT consumed by the NHL v0 model.** The pipeline path exists but 0 rows landed for the one verified game; the v0 model also does not consume sharp signals as inputs.
4. **NHL grade tier (best_angle / lean / watchlist / caution) is NOT signal-driven.** With 0 sharp_signals, every NHL pick defaults to `model_only` (verified on `pr.id=946`). The 7-category MLB grade taxonomy does not meaningfully apply to NHL today.
5. **NHL puck-line picks shown in the UI are NOT internally captured at lock AND NOT UI-labeled as context-only.** Adding them to `prediction_records` would auto-imply public tracking, which is a separate intentional product decision (Option C in §A.4). The correct fix is Option A (internal `displayed_market_snapshot` + UI label) or Option B (hide).
6. **NHL predicted per-team scores are NOT in the persisted snapshot.** They are computed live in the DTO adapter from `expected_goal_diff` + `expected_total_goals`.
7. **NHL confirmed-goalie data is NOT used.** Heuristic only (`default_most_playoff_gp`).
8. **NHL accuracy claims based on n=2 graded predictions are NOT statistically supported.** Two wins prove nothing.
9. **NHL auditor coverage does NOT exist.** Any "NHL is trusted" claim is unenforceable today.
10. **Post-game pick results are NOT surfaced in the DTO.** `result.markets.pickResult` is hardcoded null even for graded games (gap F.3.b).

---

## L. Phase-6 carry-forward from this Phase-4 audit

1. **Investigate NHL sharp_signals coverage gap.** Determine whether 0 rows for game.id=15204 is a vendor gap, a provider-client bug, or a pipeline skip. If vendor gap, document it (analogous to [[project-sharpapi-nba-coverage-gap]]).
2. **NHL puck-line: Option A or Option B (per §A.4 rule).** Do NOT just add to `prediction_records` — that pollutes public tracking. Pick one:
   - Option A: add an internal `displayed_market_snapshot` (or `snapshot_json.displayed_context_markets.puck_line`) capturing the puck-line pick/line/edge/grade/source at lock time + UI label as "Model context · Not part of official tracking" with visually distinct chip.
   - Option B: hide from `marketKeysFor("nhl")` mirroring the NBA spread move in `cba9ea5`.
   - Option C (separate decision): officially launch puck-line as a publicly tracked market with baseline disclosure. Product call, not engineering default.
3. **Surface graded results in NHL DTO.** Replace `result.markets.{pickResult, gradeUnits}: null` with real values when `prediction_grades` row exists.
4. **Replace hardcoded `sharpSignals: []` with real fetch.** Even if it returns empty 100% of the time during the SharpAPI gap, the code path must be honest — not hardcoded to an empty array.
5. **Replace hardcoded `sharpRead: "wait_no_edge_clean"` with derived value.** When sharp data is absent, use a key like `"no_sharp_data_available"` so the narrative honestly reflects the absence of data vs the presence of no edge.
6. **Persist NHL predicted scores** (`predicted_home_score`, `predicted_away_score`, `predicted_total`) at top-level on snapshot, consistent with the Phase-6 `lockSnapshotContract` (Phase 3 §N item 1).
7. **NHL confirmed-goalie ingest.** Replace `default_most_playoff_gp` heuristic with a real confirmed-starter ingest (analogous to MLB's confirmed-lineup pipeline). Surface in UI with attribution.
8. **NHL substrate completeness.** Extend `snapshot_json` to carry `signal_rows_at_lock`, `data_integrity`, `framework_grades_at_lock` in shapes compatible with the cross-sport `lockSnapshotContract`.
9. **NHL auditor adapter.** Generalize `audit-daily-edge-integrity.ts` to NHL (task #453). Categories A (lock contract), B (splits), C (lines/odds), D (tracking/grading), E (thinning) must all support NHL.
10. **NHL pre-lock refresh on its own cron, or denser tracking-refresh.** Current 1×/day refresh is adequate for Finals but inadequate for multi-game NHL slates.
11. **NHL pending-grade UPDATE regression test.** No current pending row exists to verify the path works (Phase 1 follow-up).
12. **NHL calibration scope decision.** Stanley Cup–only sample will never reach n=30 in a reasonable timeframe. Decide: wait (multi-year), expand to playoffs, or expand to regular season + revalidate confidence cap.

---

## Verdict

**NHL is PARTIAL — Stanley Cup only — TRACKING WORKED for the one verified game (CAR@VGK 2026-06-09: CAR ML WIN, OVER 5.5 WIN, correctly graded), but cannot be called TRUSTED at any scale.**

Strengths:
- End-to-end pipeline (seed → lines → model → snapshot → score ingest → grade) is wired and functioning.
- Sample game verified: tracking matches reality.
- Cron + env-gate infrastructure is correct.

Critical gaps:
- Snapshot substrate is thin (7/47 keys vs MLB benchmark).
- sharp_signals empty for the verified game.
- Puck-line is generated and displayed but not persisted or graded.
- Goalie assumption is heuristic.
- DTO has 3 hardcoded honesty gaps (sharpSignals=[], pickResult=null, sharpRead hardcoded).
- Calibration sample = 1 game.
- No NHL auditor coverage.

NHL can become TRUSTED only after Phase-6 §L items 1–12 land. Until then, NHL is **operationally functional** but **not benchmark-quality.**

---

## Cross-references

- Phase 0 inventory: `docs/audit/00-inventory.md`
- Phase 1 critical path: `docs/audit/01-active-sports-critical-path.md`
- Phase 2 NBA deep dive: `docs/audit/02-nba-model-logic-calibration.md`
- Phase 3 MLB benchmark: `docs/audit/03-mlb-benchmark-audit.md`
- Auditor / fixer design contract: `~/.claude/.../memory/project_auditor_fixer_design_contract.md`
- Phase 6 immediate roadmap: `~/.claude/.../memory/project_phase_6_immediate_roadmap.md`

## Next phase

Phase 5 — Future sports readiness. Deliverable: `docs/audit/05-future-sports-readiness.md`.
